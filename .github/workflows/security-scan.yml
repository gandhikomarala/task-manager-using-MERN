name: Security Audit & SAST

on:
  push:
    branches: [ main, develop, "feature/**" ]
  pull_request:
    branches: [ main ]

jobs:
  security-audit:
    name: Security & Secret Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Scan for sensitive credentials
        run: |
          echo "Scanning codebase for unencrypted secrets..."
          FOUND=0
          if grep -rn "BEGIN RSA PRIVATE KEY" . --exclude-dir={.git,.github,docs,node_modules}; then
            echo "::error::Private key detected!"
            FOUND=1
          fi
          if [ $FOUND -eq 1 ]; then
            exit 1
          fi
          echo "Security scan passed: zero private keys detected."
